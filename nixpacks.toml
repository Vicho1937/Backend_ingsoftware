[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && gunicorn ruta_local_backend.wsgi:application --bind 0.0.0.0:$PORT"
